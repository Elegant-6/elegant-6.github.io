# 坦克大战 · 网页小游戏 技术文档

> 本文档供 **Solo Builder（独立开发者 / AI 编码代理）** 使用。Builder 只需按本文档完整实现，即可生成一个可运行、美观、科技风的 2D 坦克大战网页游戏。
> 所有数值、玩法、模块、接口均已定义，Builder 无需自行做设计决策；如需权衡，可在文档末尾「开放问题」中标注。

---

## 1. 项目概述

| 项 | 说明 |
|---|---|
| 类型 | 2D 俯视角坦克射击（Boss 战 + 波次防守） |
| 平台 | 现代浏览器（Chrome / Edge / Firefox），桌面优先，键盘操作 |
| 技术栈 | 纯 **HTML5 + CSS3 + Vanilla JS（ES6+）**，Canvas 2D 渲染，**零第三方依赖、零联网资源**（离线可玩） |
| 美术 | 4 张坦克 PNG 贴图（已就位）+ 程序化绘制的几何特效（粒子、激光、爆炸、网格背景） |
| 音频 | Web Audio API **程序化生成**音效（不依赖音频文件） |
| 语言 | 界面文案为简体中文，代码与注释用英文标识符 + 中文注释 |

### 1.1 核心卖点
- 4 台差异化坦克，各有属性和主动技能
- 冒险模式（波次 + 基地防守 + 章节 Boss）、无尽模式
- 3 个多阶段大 Boss，技能丰富
- 科技感霓虹 UI，粒子特效，游戏内 HUD
- 本地排行榜 / 解锁系统（localStorage）

---

## 2. 目录结构（已按规范建立）

```
坦克大战/
├── index.html                  # 入口页面（所有场景 DOM 都在此）
├── assets/
│   └── tanks/
│       ├── tank_default.png    # 默认坦克（平衡型）  ← 来源 1.png
│       ├── tank_blast.png      # 爆破坦克（攻击型）  ← 来源 2.png
│       ├── tank_lightning.png  # 闪电坦克（速度型）  ← 来源 3.png
│       └── tank_guard.png      # 守护坦克（防御型）  ← 来源 4.png
│   └── sounds/                 # 预留，当前音效用 Web Audio 程序生成
├── css/
│   └── style.css               # 科技风全部 UI 样式
└── js/
    ├── config.js               # 全部游戏常量与数据表（唯一改数值的地方）
    ├── main.js                 # 入口：游戏循环、场景状态机、初始化
    ├── input.js                # 键盘输入（WASD/方向键/空格/技能键/暂停）
    ├── tank.js                 # Tank 基类 + Player + Enemy 逻辑
    ├── bullet.js               # 子弹系统（穿透 / 爆炸溅射 / 激光 / 追踪弹）
    ├── boss.js                 # Boss 类 + 技能 AI + 多阶段血条
    ├── map.js                  # 战场地图、墙体、基地、刷新点
    ├── item.js                 # 道具掉落与拾取逻辑
    ├── particles.js            # 粒子/爆炸/尾焰/屏幕震动
    ├── audio.js                # Web Audio 程序化音效
    └── ui.js                   # HUD、菜单、选角、结算、排行榜等全部界面逻辑
```

**图片映射说明（重要）**：`1.png`→默认坦克、`2.png`→爆破坦克、`3.png`→闪电坦克、`4.png`→守护坦克。原图均为 32bpp ARGB（透明底）大尺寸方形 PNG（约 873~1035px），游戏中需等比缩放使用：战场内按 48×48 绘制，选角界面卡片按 ~200×200 展示。图片加载失败时必须回退为程序绘制的同色几何坦克，保证可玩。

---

## 3. 角色设计（4 台坦克）

数据集中在 `config.js` 的 `TANKS` 表中，逻辑层不写死数值。

| 属性 | 默认坦克 | 爆破坦克 | 闪电坦克 | 守护坦克 |
|---|---|---|---|---|
| 类型 | 平衡型 | 攻击型 | 速度型 | 防御型 |
| 生命 HP | 120 | 100 | 80 | 220 |
| 移速 px/s | 180 | 140 | 260 | 130 |
| 攻击力 | 25 | 45 | 18 | 22 |
| 射速 发/s | 2.0 | 1.2 | 3.5 | 1.8 |
| 弹速 px/s | 480 | 420 | 520 | 460 |
| 护甲（减伤） | 0 | 0 | 0 | 3（每发至少扣 1） |
| 贴图 | tank_default.png | tank_blast.png | tank_lightning.png | tank_guard.png |
| 主题色 | 蓝白 | 橙红 | 青黄 | 蓝绿 |

### 3.1 主动技能（均有冷却，HUD 图标倒计时转圈）
- **默认坦克 ·「三连发」** 冷却 8s：下一次开火同时射出 3 发子弹（呈 0/-10°/10°）。
- **爆破坦克 ·「核弹炮」** 冷却 15s：发射一枚大型炮弹，命中后大范围爆炸（半径 3 格），伤害 120。
- **闪电坦克 ·「闪现冲刺」** 冷却 6s：向当前移动方向高速冲刺 150px（0.15s），期间无敌，撞到的敌人各受 60 伤害。
- **守护坦克 ·「能量护盾」** 冷却 15s：生成可吸收 60 点伤害的护盾，持续 10s（盾量优先于护甲结算）。

### 3.2 坦克通用规则
- 坦克有朝向，贴图顶边为前方；移动方向 = 朝向，渲染时绕中心旋转贴图。
- 贴图绘制大小：战场 48×48（等比缩放原图）；死亡爆炸 30 粒子 + 屏幕震动 6px。
- 初始朝上（+y 负方向），旋转角度 = `atan2(dx, dy)`（约定 0° 朝上，顺时针）。

---

## 4. 玩法设计

### 4.1 游戏模式
1. **冒险模式（核心）**：3 章 × 5 关。每关若干波敌人 + 一个需要守护的「基地」；关底第 5 关为章节 Boss 战。胜利条件：消灭全部敌人且基地存活。
2. **无尽模式**：无限波次，波次强度随时间/波数递增，生存计分（击杀分 × 连击倍率）。
3. **Boss 挑战**：直接挑战已解锁的 Boss，用于练习与刷分。

### 4.2 敌人类型（`config.js` → `ENEMIES`）

| 敌人 | HP | 移速 | 攻击 | 弹速 | 行为描述 |
|---|---|---|---|---|---|
| 侦察兵 Scout | 30 | 220 | 撞击 10 | - | 不射击，直冲玩家 |
| 突击兵 Grunt | 60 | 120 | 20 | 400 | 常规敌人，追击 + 射击 |
| 重装兵 Heavy | 200 | 70 | 35 | 320 | 血厚攻高，减速 |
| 自爆兵 Kamikaze | 40 | 170 | 自爆 60 | - | 靠近后自爆，范围伤害 |
| 狙击手 Sniper | 50 | 90 | 40 | 800 | 停下瞄准后高弹速射击 |
| 精英兵 Elite | 120 | 150 | 25 | 450 | 会 30% 概率闪避子弹 |

- 敌人死亡 20% 概率掉落道具；Boss 死亡 100% 掉落 + 宝箱。
- 每波敌人组合由 `LEVELS` 配置，难度逐波递增。

### 4.3 道具系统（`config.js` → `ITEMS`）

| 道具 | 效果 |
|---|---|
| 火力强化 Ⅴ | 火力等级 +1（最高 5 级；1/2 级单发，3 级双发，4/5 级三发，伤害递增） |
| 护盾 | 吸收 80 伤害护盾，持续 10s |
| 生命回复 | +30 HP（不超过上限） |
| 速度强化 | 移速 +40%，持续 8s |
| 双发 | 10s 内每次开火双发 |
| 穿透弹 | 10s 内子弹穿透敌人 |
| 冻结 | 冻结全场非 Boss 敌人 3s |
| 核弹 | 立即消灭当前场上所有非 Boss 敌人 |

- 道具为地面光球，带颜色光晕 + 图标；拾取后 HUD 显示倒计时。

### 4.4 评分与连击
- 击杀得分：Scout 100、Grunt 200、Heavy 400、Kamikaze 150、Sniper 300、Elite 500、小 Boss 2000、章节 Boss 10000。
- 3s 内连续击杀触发连击，倍率 ×1.0~×5.0（HUD 显示 COMBO）。
- 通关星级：★ 三星（血量>70% & 无基地受损）、★★ 五星（血量>90% & 基地无伤）。

---

## 5. Boss 设计

### 5.1 通用规则
- Boss 血条常驻屏幕顶部中央，带能量感分割纹；入场播放 2s 入场动画 + 警报音。
- **多阶段**：血量 ≤75% / 50% / 25% 时进入下一阶段——全屏红色警告 1s、短暂无敌 1.5s、新增技能、贴图色调转红。
- Boss 免疫冻结 / 穿透效果；死亡掉落宝箱 + 大爆炸特效。

### 5.2 技能池（每个 Boss 从池中组合）
1. **扇形扫射**：朝玩家方向扇形 7 发
2. **环形弹幕**：360° 均匀 24 发
3. **激光**：锁定玩家方向 0.6s 蓄能（警示线）后持续激光 1.2s
4. **召唤**：召唤 3 只突击兵
5. **冲撞**：短暂蓄力后向玩家位置冲锋
6. **追踪导弹**：发射 3 枚追踪玩家的导弹（转弯率受限）
7. **陨石轰炸**：随机 5 个区域落弹预警（红色圈）

### 5.3 Boss 原型（3 个，几何程序绘制：多层多边形 + 发光核心）

| Boss | HP | 移速 | 阶段数 | 技能 | 说明 |
|---|---|---|---|---|---|
| 铁甲巨兽 IronGolem | 3000 | 55 | 2 | 扇形扫射、冲撞、召唤 | 近战威胁，召唤杂兵 |
| 电磁主宰 VoltLord | 2500 | 85 | 2 | 环形弹幕、追踪导弹、激光 | 中程弹幕型 |
| 末日要塞 DoomFortress | 5000 | 40 | 3 | 全部技能 + 陨石轰炸 | 终章大 Boss |

---

## 6. UI / UX 设计（科技风）

### 6.1 视觉规范
- **背景**：深空蓝黑 `#0a0e17`；战场网格线 `rgba(0,240,255,.08)`；背景缓慢流动粒子（Canvas）。
- **主色**：霓虹青 `#00f0ff`；辅色：霓虹紫 `#b026ff`；警告红 `#ff2e4d`；能量黄 `#ffd23f`。
- **组件**：半透明深色面板（`rgba(10,14,23,.85)` + `backdrop-filter: blur(8px)`）、霓虹发光描边、圆角、扫描线/噪点叠加层、标题加宽字距大写英文 + 中文副标。
- **动效**：按钮 hover 发光、按下缩放；面板淡入 + 缩放弹出；数值跳动动画（伤害飘字）；屏幕震动。
- **字体**：优先 `"Orbitron", "Segoe UI", "Microsoft YaHei", sans-serif`（Orbitron 未安装则用系统字体，勿引入网络字体）。

### 6.2 页面（场景）清单
1. **主菜单**：游戏 LOGO（霓虹标题）、开始游戏、模式选择、排行榜、设置（音量 / 基地开关 / 重置存档）。
2. **角色选择**：4 张坦克卡片横向排列，展示贴图 + 属性数值条（生命/火力/速度/防御/射速 雷达图）+ 技能说明；左右切换、回车确认；未解锁角色显示锁。
3. **游戏内 HUD**：
   - 左上：头像 + HP 条 + 护盾条
   - 右上：得分 + COMBO
   - 顶中：关卡/波次、Boss 血条（Boss 战时）
   - 底部：技能图标（冷却转圈）+ 道具倒计时
   - 右下：小地图（可选，基地/玩家/Boss 光点）
   - 暂停键（Esc/P）弹出暂停面板
4. **通关结算**：得分、击杀数、连击最高、星级动画、下一关 / 重试按钮。
5. **失败结算**：原因（基地被毁 / 坦克被击毁）、重试 / 回主菜单。
6. **排行榜**：localStorage Top10，显示坦克头像 + 分数 + 模式。

### 6.3 操作（键盘）
| 按键 | 功能 |
|---|---|
| WASD / 方向键 | 移动（决定朝向） |
| 空格 | 射击（按住连发） |
| J / Shift | 主动技能 |
| P / Esc | 暂停 |

---

## 7. 技术实现细节

### 7.1 渲染架构
- Canvas 逻辑尺寸 **960×960**（战场 20×20 格，格子 48px），按窗口等比缩放（CSS `max-width` + 居中，保持 1:1 正方形）。
- 渲染顺序（每帧）：背景网格 → 战场地图 → 道具 → 坦克 → 子弹 → Boss → 粒子 → 半透明边框/警示层；**HUD 与菜单用 DOM/CSS 叠加**（不画在 Canvas 上）。
- 游戏循环：`requestAnimationFrame` + 固定时间步 `1/60s`（累积器），`dt` 驱动所有移动，保证不同刷新率手感一致。
- 画面反馈：命中白色闪光、爆炸粒子、屏幕震动（随机偏移 2 帧）、伤害飘字。

### 7.2 坦克贴图使用规则（关键）
- 启动时用 `Image` 预加载 4 张贴图到内存（含 `onerror` 回退绘制函数）。
- 战场绘制：`ctx.save(); ctx.translate(x,y); ctx.rotate(angle); ctx.drawImage(img, -24, -24, 48, 48); ctx.restore();`
- **方向约定**：贴图内容「头部/炮口」朝上为初始朝向；`angle = atan2(dir.x, -dir.y)`（使贴图顶部对准移动方向）。
- 若某张贴图实际内容朝向不同，`config.js` 中 `TANKS[].imgRotateOffset` 可做 ±90° 修正（默认 0）。
- 选角界面使用同一贴图，放大至 200×200 并加发光描边与属性面板。

### 7.3 碰撞检测
- 坦克/子弹/墙体：AABB（`x,y,w,h`）。
- 子弹与坦克：AABB 即可；爆炸范围伤害用圆心距离判定。
- 优化：网格分桶（key = `格x_格y`）加速，但规模小，全量遍历亦可。

### 7.4 数据驱动
`config.js` 导出以下表，Builder 必须全部走数据：
```js
export const TANKS = {
  default:   { key:'default',   name:'默认坦克', img:'assets/tanks/tank_default.png',   hp:120, speed:180, attack:25, fireRate:2.0, bulletSpeed:480, armor:0, color:'#00f0ff', skill:{ key:'triple', cooldown:8 } },
  blast:     { key:'blast',     name:'爆破坦克', img:'assets/tanks/tank_blast.png',     hp:100, speed:140, attack:45, fireRate:1.2, bulletSpeed:420, armor:0, color:'#ff5a2e', skill:{ key:'nuke',   cooldown:15 }, passive:'splash' },
  lightning: { key:'lightning', name:'闪电坦克', img:'assets/tanks/tank_lightning.png', hp:80,  speed:260, attack:18, fireRate:3.5, bulletSpeed:520, armor:0, color:'#ffd23f', skill:{ key:'dash',    cooldown:6 } },
  guard:     { key:'guard',     name:'守护坦克', img:'assets/tanks/tank_guard.png',     hp:220, speed:130, attack:22, fireRate:1.8, bulletSpeed:460, armor:3, color:'#00ffb0', skill:{ key:'shield',  cooldown:15 } },
};
export const ENEMIES = { scout:{hp:30,speed:220,attack:10,score:100,img:null}, /* ... */ };
export const BOSSES = { ironGolem:{...}, voltLord:{...}, doomFortress:{...} };
export const ITEMS = { power:{...}, shield:{...}, heal:{...}, speed:{...}, double:{...}, pierce:{...}, freeze:{...}, nuke:{...} };
export const LEVELS = [ { chapter:1, waves:[ [enemyKeys], ... ], map:0, boss:null }, ... ];
export const GAME = { tile:48, cols:20, rows:20, fixedStep:1/60, ... };
```

### 7.5 程序化音效（audio.js，Web Audio）
- 开火：短促方波（频率 220→60Hz，0.08s）
- 爆炸：白噪声低频衰减（0.4s）
- 命中：短咔嗒（带通噪声）
- 拾取：上行琶音（3 音）
- 技能：扫描音（振荡器扫频）
- Boss 警告：双音警报
- 背景氛围：低频正弦 pad 循环（可开关），音量默认 40%

### 7.6 场景状态机
- `app.scene ∈ { menu, select, playing, paused, victory, defeat, leaderboard }`。
- `main.js` 持有全局单例 `app`：`{ scene, player, enemies[], bullets[], items[], particles[], wave, score, combo, settings }`。
- 切换场景：停止/恢复游戏循环；DOM 面板显隐 + 动画；数据在 `startGame()` 时重置。
- 关卡数据在波次之间只重置敌人物件，不清玩家状态。

### 7.7 本地存储（localStorage）
- 键：`tz_players`（解锁状态）、`tz_highscores`（Top10）、`tz_settings`（音量、基地开关）、`tz_progress`（最高章节）。
- 每次通关/结算即时写入；`ui.js` 提供「重置存档」入口。

---

## 8. 开发阶段规划（Builder 建议按序实现）

1. **P1 骨架**：目录文件全建齐；`main.js` 循环 + 场景切换；`index.html` 全部页面骨架 DOM；`style.css` 基础科技风（背景、字体、面板）；单坦克 WASD 移动 + 贴图旋转 + 空格射击 + 子弹 + 墙体 AABB 碰撞。
2. **P2 内容**：4 台坦克差异属性 + 3 个主动技能；5~6 种敌人 AI；道具掉落与拾取；粒子爆炸。
3. **P3 玩法**：冒险模式波次 + 基地防守 + 胜负判定；无尽模式；本地排行榜。
4. **P4 Boss**：Boss 类 + 7 种技能 + 多阶段血条 + 3 个 Boss 原型；Boss 挑战模式。
5. **P5 润色**：全部 UI 动效、程序音效、屏幕震动、伤害飘字、扫 960×960 适配、手感调优、离线验证。

---

## 9. 验收清单（Builder 交付前自检）

- [ ] `index.html` 双击即可离线运行，浏览器控制台无报错
- [ ] 4 台坦克可选、贴图正确显示、属性与技能差异可明显感知
- [ ] 冒险模式可通关至少 1 章（含 Boss 战），胜利/失败/重试流程完整
- [ ] Boss 有顶部血条、≥2 阶段、每 Boss ≥3 种技能
- [ ] 科技风 UI：霓虹、粒子、动效、HUD、选角、结算、排行榜齐全
- [ ] 道具、连击、评分、解锁、localStorage 存档均生效
- [ ] 键盘操作流畅，60fps（现代 PC）

---

## 10. 开放问题（若需 Builder 权衡，请在此标注）

1. 坦克贴图原图实际朝向未知：请按「炮口朝上」假设实现，若图方向不对用 `imgRotateOffset` 修正。
2. Boss 无现成美术，使用程序化几何绘制（可后续替换 boss.png）。
3. 冒险模式共 3 章是否够？如需扩展关卡，修改 `LEVELS` 表即可。
4. 是否增加移动端触屏虚拟摇杆？（默认仅桌面键盘，移动端仅提示“请使用电脑”）。
